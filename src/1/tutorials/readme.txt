1. Master(logged)/Slave(anonymous) public read/write model
2. Master(logged)/Slave(logged) private read/write model
3. Multiple domains
4. Multiple domains with subdomains and pagination
5. Firebase pub/sub
6. Upload/download text/plain to bucket.
7. Upload/download canvas to bucket.
8. Upload/download file to bucket.